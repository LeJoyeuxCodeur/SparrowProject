package model.player;

/**
 * This class represents a non player character
 */
public class NPC extends Player{
	/**
	 * Build a non player character
	 * @param money {@link Integer}
	 */
	public NPC(Integer money) {
		super(money);
	}
}